[pytest]
testpaths = tests
addopts = -v
markers =
    smoke: temel açılış testleri
    regression: genel regresyon
    flaky: demo kararsız testler (add-to-cart gibi)
    boundary: sınır değer testleri
    security: güvenlik odaklı input testleri
